<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Tabi.StopsDebugPage">
    <ContentPage.Content>
        <ListView x:Name="StopsView" ItemSelected="OnSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="Stop" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <!--    <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <StackLayout AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1">
                <ListView x:Name="StopsView" ItemSelected="OnSelected">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="Stop" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
            <StackLayout AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1" x:Name="actIndicatorContainer">
                <ActivityIndicator x:Name="actIndicator" VerticalOptions="Center" HorizontalOptions="Center" />
            </StackLayout>-->
        <!--        </AbsoluteLayout>-->
    </ContentPage.Content>
</ContentPage>